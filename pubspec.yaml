name: fruits_app
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

  # Using this fork as alchemist is not currently
  # compatible with Flutter 3.13
  alchemist:
    git:
      url: https://github.com/ABausG/alchemist/
      ref: flutter-3-13

  #TODO: Resolve confusion cyclic dependency confusion.
  widgetbook_fruits_app:
    path: widgetbook

flutter:
  uses-material-design: true
