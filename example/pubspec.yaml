name: fruits_app
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

  # For golden testing:
  alchemist: ^0.7.0

  # TODO: Resolve cyclic dependency confusion.
  widgetbook_workspace:
    path: widgetbook
  widgetbook_4:
    path: ../widgetbook_4

flutter:
  uses-material-design: true
